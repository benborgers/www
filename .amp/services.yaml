services:
  web:
    command: 'OPENAI_API_KEY="${OPENAI_API_KEY:-orb-development-placeholder}" npm run dev -- --host 0.0.0.0 --port "$PORT"'
    portal:
      title: Ben Borgers
      description: Astro development server with Portal-safe origin handling
