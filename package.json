{
  "name": "www",
  "scripts": {
    "build": "rm -rf public && mkdir public public/blog public/static && cp src/static/* public/static && node src/build"
  },
  "dependencies": {
    "autoprefixer": "^9.7.4",
    "cssnano": "^4.1.10",
    "html-minifier": "^4.0.0",
    "node-fetch": "^2.6.0",
    "postcss": "^7.0.26",
    "sitemap": "^5.1.0"
  }
}
