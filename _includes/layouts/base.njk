<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        {% if title %}
            {{ title | safe }} -
        {% endif %}
        Ben Borgers
    </title>
    <link rel="icon" href="https://emojicdn.elk.sh/🐙" />
    <link rel="stylesheet" href="/style.css" />
</head>
<body class="font-sans antialiased p-4 sm:p-6 pb-16 sm:pb-16 text-gray-700 bg-white">
    <div class="max-w-screen-md mx-auto">
        <header class="mb-24 flex items-center justify-between">
            <a href="/" class="inline-block font-semibold text-blue-600">Ben Borgers</a>

            <div class="flex space-x-3">
                {% for item in nav %}
                    <a href="{{ item.url }}" class="inline-block duration-150 {% if page.url === item.url %} text-gray-800 {% else %} text-gray-400 hover:text-gray-700 {% endif %}">{{ item.name }}</a>
                {% endfor %}
            </div>
        </header>

        <main class="max-w-screen-sm mx-auto">
            {{ content | safe }}
        </main>

        {% if page.url.startsWith('/post') %}
            <footer class="border-t border-gray-200 mt-16 pt-2">
                <p class="text-gray-400">
                    👋 I love getting questions or replies!
                    <a href="mailto:benborgers@hey.com" class="underline duration-150 hover:text-gray-600">benborgers@hey.com</a>
                </p>
            </footer>
        {% endif %}
    </div>
</body>
</html>
