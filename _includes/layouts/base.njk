<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        {% if title %}
            {{ title | smartQuotes }} -
        {% endif %}
        Ben Borgers
    </title>
    <link rel="stylesheet" href="/style.css" />
    <link rel="icon" href="https://emojicdn.elk.sh/🐙" />
    {% if content.includes('katex-html') %}
        <link rel="stylesheet" href="https://unpkg.com/katex@0.12.0/dist/katex.min.css" />
    {% endif %}
</head>
<body
    class="font-sans antialiased text-gray-700 bg-white max-w-screen-sm mx-auto min-h-screen grid pt-4 px-4 pb-6 md:px-0 md:pt-6"
    style="grid-template-rows: max-content 1fr max-content"
>
    <header class="mb-16 sm:mb-24 flex items-center justify-between">
        <div>
            <div class="w-5 h-5 bg-gradient-to-tr from-red-300 to-blue-400 rounded-full"></div>
            <a href="/">
                <div class="py-1 px-2 rounded-lg bg-white border border-gray-200 shadow-lg -mt-3 ml-2">
                    <p class="inline-block font-semibold text-gray-700">Ben&nbsp;Borgers</p>
                </div>
            </a>
        </div>

        <nav class="sm:flex space-x-3">
            {% for item in nav %}
                <a
                    href="{{ item.url }}"
                    class="block text-right duration-150 font-medium transition-colors {% if page.url === item.url %} text-gray-700 {% else %} text-gray-400 hover:text-gray-700 {% endif %}"
                >{{ item.name }}</a>
            {% endfor %}
        </nav>
    </header>

    <main>
        {{ content | safe }}
    </main>

    <footer class="border-t border-gray-200 mt-16 pt-4 flex items-center justify-between">
        <p class="text-gray-400">
            If I can help or you wanna chat:
            <a href="mailto:{{ email }}" class="underline duration-150 hover:text-gray-600">{{ email }}</a>
        </p>
        <p>👋</p>
    </footer>
</body>
</html>
