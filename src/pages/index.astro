---
const res = await fetch('https://api.github.com/repos/benborgers/cms/issues/1', {
  headers: { 'Accept': 'application/vnd.github.VERSION.html+json' }
})

const json = await res.json()
---

<div set:html={json.body_html} />
