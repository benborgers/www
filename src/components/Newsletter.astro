<div class="p-4 bg-gray-50 border border-gray-100 rounded-xl">
  <p class="font-bold text-gray-800 font-width-5">
    Subscribe to my newsletter!
  </p>
  <p class="mt-1 text-gray-500 max-w-sm">
    A weekly round-up of new {
      Astro.url.pathname === "/posts" ? (
        "blog posts"
      ) : (
        // prettier-ignore
        // Prettier keeps trying to put this on separate lines, which adds a space after the underlined text.
        <a href="/posts" class="underline decoration-gray-400 hover:text-rose-500 hover:decoration-rose-500 transition-colors">blog posts</a>
      )
    } and updates to projects I’m working on.
  </p>
  <form
    action="https://buttondown.email/api/emails/embed-subscribe/benborgers"
    method="post"
    target="popupwindow"
    onsubmit="window.open('https://buttondown.email/benborgers', 'popupwindow')"
    class="mt-3 flex items-end justify-between gap-x-4 max-sm:flex-col gap-y-2"
  >
    <div class="w-full">
      <label for="email" class="block w-max text-sm font-medium text-gray-400">
        What’s your email address?
      </label>
      <input
        type="email"
        name="email"
        id="email"
        class="mt-1 sm:mt-0.5 py-1.5 w-full rounded-lg text-gray-800 font-medium border-gray-200 focus:border-rose-500 focus:ring-rose-500 transition-colors"
      />
    </div>
    <button
      class="bg-rose-500 text-white px-4 py-[0.57rem] rounded-lg text-sm font-semibold font-width-5 max-sm:w-full"
    >
      Subscribe
    </button>
  </form>
</div>
