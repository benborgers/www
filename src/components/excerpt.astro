---
interface Props {
  markdown: string;
}

import removeMarkdown from "remove-markdown";

const { markdown } = Astro.props;
---

<div class="line-clamp-2">
  <p>{removeMarkdown(markdown).substring(0, 200)}</p>
</div>
